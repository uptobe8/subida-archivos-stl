name: Fix and Generate Legadia Servicios Premium

on:
  push:
    paths:
      - 'chatgpt-output/legadia/servicios-premium/fix_and_generate.py'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install python-docx
      - run: python chatgpt-output/legadia/servicios-premium/fix_and_generate.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add chatgpt-output/legadia/servicios-premium/output
          git diff --cached --quiet || git commit -m "Generate Legadia Servicios premium Divi 4 files"
          git push
